---
steps:
- name: hashicorp/terraform
  dir: deployments/networking/${_ENV_NAME}
  args:
  - init
- name: hashicorp/terraform
  dir: deployments/networking/${_ENV_NAME}
  args:
  - apply
  - -auto-approve
